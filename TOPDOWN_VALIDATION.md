# 俯视栅格管线 — 标定与验证记录

`topdown_pipeline.py`（核心：backproject → 地面拟合 → 高度过滤 → 10×9俯视栅格）+
`validate_distance.py`（批量跑测试集，对比估距 vs 卷尺真实距离）+
`check_balloon_depth.py`（直接在深度图上采样气球像素区 vs 背景，判断气球是否可辨认）。

用官方 `Depth-Anything-V2/metric_depth/run.py --save-numpy` 产出的米制深度 `.npy` 作为输入。

## 标定

拍一张正对着已知宽度物体（门/柜）、已知距离的照片，量出物体在照片里的像素宽，代入：

```
fx = px × dist(m) / real_width(m)
```

**当前值：fx = 3260px**（复测：门扇 1467px @ 3072宽基准 × 2.0m / 0.90m ≈ 3260px，与 Mate 50 Pro 主摄
27mm 等效焦距理论值偏差约6%，量级吻合，标定已确认，无需重拍）。

## 测试集协议

同一场地、实验用的胸挂高度和角度拍 8-10 张，每张记录真实距离：空场×2、椅子在若干已知距离、
椅子贴墙、悬挂/地面物体×2、混合场景。图片放 `pics/`，跑 `run.py` 前建议先把中文文件名复制成
ASCII 名（`pics/ascii/`）——Git Bash 传中文参数给 Windows Python 会把 argv 乱码掉，`cv2.imread`
读不到文件，且报错信息本身也是乱码，容易误判成别的问题。复制脚本模板见 `prep_new_pics.py`
（在 Python 文件内部写死中文路径字符串再用 `shutil.copy2`，不经过命令行参数，就不会乱码）。

## 地面拟合算法（`ransac_ground`，两轮迭代 → 三轮迭代 + 非对称容差）

1. 取画面底部 `GROUND_IMG_FRAC` 比例的点作候选（**0.45 → 0.25**，比例越大，近距离物体越容易占满候选带底部、被误拟合进地面）。
2. RANSAC 粗定位平面（300次随机3点采样取最大共识）。
3. 最小二乘精修一次（3×3 协方差矩阵最小特征值方向）。
4. **REFIT_ROUNDS=3** 轮迭代剔除+精修：每轮用当前平面重新计算全部候选点的**带符号**残差，非对称剔除——
   地面以下容忍到 **-15cm**（黑布/地毯褶皱），地面以上只容忍 **+5cm** 就剔除（专门甩掉椅子腿这类贴地但更高的点）。

## 尺度锚定（`validate_distance.py`）

胸挂实测高度 `CAM_H_TRUE = 1.40m`。流程：原始深度先拟合一次地面拿到 `cam_h_raw` →
`scale = 1.40 / cam_h_raw` → 深度整体乘 scale → 重新走一遍完整流程（拟合出的 cam_h
应精确回到 1.40，作为交叉验证）。

**健康指标**：每批照片汇总 `scale` 的离散度（CV = std/mean）。同一个地面场景下 scale
理论上应该高度一致——**CV < 10% 才算地面拟合干净**，否则说明不同帧的地面拟合互相不一致
（材质/光照/近处物体污染都可能是原因），后面所有基于该 scale 的结论都不可信。

## 当前结果（黑布地面测试集，8张，fx=3260，两轮→三轮+非对称收紧后）

| 场景 | cam_h_raw | scale | 估距 | 真实 | 误差 |
|---|---|---|---|---|---|
| 空场1/2 | 1.57 | 0.892 | — | — | — |
| 椅子2m | 2.30 | 0.609 | 1.66 | 2.00 | -17.0% |
| 椅子3m | 2.05 | 0.684 | 2.58 | 3.00 | -14.0% |
| 椅子4m | 2.16 | 0.649 | 3.50 | 4.00 | -12.5% |
| 椅子5m | 1.82 | 0.770 | 4.09 | 5.00 | -18.2% |

**`scale` CV = 14.9%，超过10%健康线，判定这批数据的地面拟合不一致。**

**关键发现：把候选带从45%收紧到25% + 非对称[-15,+5]cm剔除 + 3轮迭代，结果几乎没变**
（cam_h_raw 2.31→2.30，2.08→2.05，2.18→2.16，1.85→1.82，全部原地不动，chair_5m
反而从 -9%误差恶化到 -18%）。RANSAC 本身的内点率全程 98-100%——说明候选带里的点
**本来就高度自洽地支持同一个（偏高的）平面**，不是"椅子腿混进候选带被平均掉"这种可以靠
调阈值解决的问题。这把嫌疑从"候选点选取算法"转向"整个候选带的深度值本身就有系统性偏差"，
和气球测试的发现（近距离~1-2m处 metric depth 绝对尺度明显失准，见下）指向同一个方向：
**可能是黑色反光/褶皱塑料布这种材质本身让 Depth Anything V2 在近距离测不准，不是俯视管线算法的锅。**

## 决定性对照实验结果（2026-08-09，结论：黑色材质是主因，基本坐实）

Hsinlung 补拍了3张照片（相机高度同样约1.4m），放进 `pics/`（原文件名带空格和中文，脚本跑之前先用
`prep_new_pics.py` 复制成 ASCII 名进 `pics/ascii/`）：

- `空场 n.jpg` → `empty3.jpg`
- `椅子3m n.jpg` → `chair_3m_plain.jpg`
- `气球1m n.jpg` → `balloon_1m_plain.jpg`

**重要：这三张照片的地面不是均匀的"普通地面"**，实际是浅蓝色环氧地坪（画面中段，反光但不是黑色）+
近处/右侧一块深色橡胶垫（带白色划线标记，肉眼看颜色深、材质和黑布类似）。这个混合场地反而成了
一次很干净的对照实验，因为同一张照片里同时有"浅蓝地坪"和"深色材质"两种区域。

### 结果 1：物体测距精度——浅蓝地坪区域大幅改善

| 场景 | 地面材质 | cam_h_raw | scale | 估距 | 真实 | 误差% |
|---|---|---|---|---|---|---|
| 椅子3m（黑布） | 黑布 | 2.05 | 0.684 | 2.58 | 3.00 | **-14.0%** |
| 椅子3m（新拍） | 浅蓝地坪 | 2.04 | 0.685 | 2.94 | 3.00 | **-2.1%** |

椅子本身落在浅蓝地坪范围内（不挨着深色胶垫），估距误差从 -14% 降到 -2.1%，接近标定/量距误差量级。
`cam_h_raw`/`scale` 数值本身几乎没变（下面第3点解释原因），但这不影响物体本身的测距精度——说明
**只要目标物体所在的地面区域不是深色材质，物体的绝对深度就准。**

### 结果 2：气球测距——数值直接匹配俯仰几何预测，比黑布组好一个量级

气球是躺在地上的长条气球，标称水平距离1m，相机高度1.4m，如果模型深度是"点到相机的直线距离"
（而不是垂直地面的水平距离），几何预测值应该是 √(1.4² + 1²) ≈ **1.72m**，不是1m。

| 场景 | 地面材质 | 气球区中位深度 | 背景中位深度 | vs 1.72m几何预测 |
|---|---|---|---|---|
| 近气球（黑布组，标称1m） | 黑布 | 3.11m | 3.38m | 差 ~1.8x |
| 混合图气球（黑布组，标称1m） | 黑布 | 3.62m | 4.09m | 差 ~2.1x |
| 气球（新拍，标称1m） | 浅蓝地坪 | **1.63m** | 3.74m | **误差仅约-5%，直接对上** |

（bbox 选取：先用颜色阈值 R>G+15 & B>G+5 在原图上定位气球像素，人工核实后专门收在 x<2050 —— 确认
这段气球全程压在浅蓝地坪上、不挨深色胶垫交界处，避免材质混淆。）

浅蓝地坪上的气球深度几乎精确落在纯几何预测值上，这是本次对照实验里最干净的一个信号：
**Depth Anything V2 在浅蓝反光地坪上的绝对尺度是准的，问题确实出在黑色/深色地面材质本身。**

### 结果 3：但 `scale`/`cam_h_raw` 依然没回到 ~1.0——原因已查清，不是新问题

新拍两张的 `cam_h_raw`（2.04-2.11m）、`scale`（0.664-0.685）、CV（14.0%）几乎和黑布组（14.9%）
一样没改善，乍看像是"黑布假设被推翻"，但逐行检查了 `GROUND_IMG_FRAC=0.25` 对应的候选带
（画面底部25%，即 y∈[3072,4096]）在这两张新照片里实际落在什么材质上：

```
y=3000  RGB≈(120,161,185)  浅蓝地坪
y=3100  RGB≈(63,68,84)     已经是深色胶垫
y=3200~4000  RGB≈(60-80, 60-85, 70-90)  全程深色胶垫
```

**候选带的材质分界线正好卡在 y≈3100-3200，几乎等于25%候选带的起点（y=3072）**——也就是说，
这两张"对照组"照片里，RANSAC 用来拟合地面的候选点，几乎**全部落在画面最下方那条深色橡胶垫上**，
和浅蓝地坪基本没关系。黑布组的候选带同样是"最靠近相机的地面条带"，材质也是深色。两组的地面拟合
候选带材质其实是同一类问题（深色反光地面），只是这次深色区域缩小到了画面最下方一条，没有覆盖椅子
和气球所在的浅蓝区域——这正好解释了为什么**物体测距（结果1、2）大幅改善，而 `scale`/`cam_h_raw`
（依赖的是画面最下方candidate带）完全没变**：两者用的是不同区域的深度值，前者干净、后者依然踩坑。

**结论：不是"黑布假设被推翻"，反而是同一个假设在同一批数据里被双重验证**——深色材质区域的深度
不准（候选带→cam_h_raw/scale 依然偏），非深色区域的深度准（椅子、气球→误差大幅下降）。

### 总体判定

**黑色/深色反光地面材质导致 Depth Anything V2 近距离绝对尺度失准，这个假设成立**，俯视管线算法
本身（地面拟合、高度过滤、栅格化）没有问题——只要喂给它的深度输入本身是准的，管线输出就准
（椅子3m误差-2.1%已经证明）。

### 下一步（如果还想彻底坐实/量化，非必须）

1. 补拍一张**从头到尾（包括画面最下方25%候选带）都在浅色/非反光地面上**的照片（人站的位置要让
   深色胶垫完全不入镜，包括脚下），重新验证 `scale` 能否回到 ~1.0——这才是对"地面拟合算法本身"的
   干净测试，目前三次尝试（黑布组、新拍组）候选带都不巧踩在深色材质上。
2. 如果暂时没有更多拍摄机会，现有结果已经足够支撑"以后测试集/实际部署选普通浅色地面拍摄，避开
   黑色反光材质"这个操作结论，可以先按这个结论往下走（比如开始接入俯视栅格到实际胸挂设备），
   不必卡在这里继续拍照验证。

## 气球检测（`check_balloon_depth.py`）

气球是**躺在地上的长条气球**，不是悬挂物——不能用"离地高度窗口"筛选（最早的假设就是错的）。
直接在深度 `.npy` 上采样气球像素区 vs 紧邻背景区的中位深度：

| 场景 | 气球区中位深度 | 背景中位深度 | 差 |
|---|---|---|---|
| 近气球(标称1m) | 3.11m | 3.38m | -0.27m (-8.1%) |
| 远气球(标称2m) | 5.56m | 5.11m | +0.45m (+8.7%，方向反了，测不出来) |
| 混合图气球(标称1m) | 3.62m | 4.09m | -0.47m (-11.4%) |

两个近气球能看出"比背景近"的相对信号，但**绝对深度值离标称值差了约2-3倍**（1m测成3.1-3.6m），
这个量级不是俯仰角几何投影能解释的，指向 metric depth 模型近距离绝对尺度失准——见上面
"决定性对照实验"。

## 环境问题（重要，与算法本身无关，但会让人误判成算法 bug）

`lingtouch` conda 环境的 numpy **LAPACK 库整体损坏**：`np.linalg.{svd,eigh,inv,det}`
或任何 N≳1000 的矩阵-向量乘法（`@`/`np.dot`）调用都会**静默崩溃**（退出码127，无 traceback，
终端啥也不打印）。诊断过程见本文件对应的对话记录，简单说是逐层排除法：先怀疑内存爆炸
（SVD `full_matrices=True` 默认值确实有这个坑，已修），修完还是崩，最后发现连
`np.linalg.inv(np.eye(3))` 都崩，才确认是 LAPACK 本身坏了，跟数组大小/写法无关。

**影响范围不止这两个脚本**——matplotlib 画图（`savefig`）内部要算仿射变换矩阵的逆，
同样会崩，所以 `topdown_pipeline.py` 里 `--outdir` 那段四联图可视化目前跑不通。

**当前绕开方式**（已在 `topdown_pipeline.py` 里实现，不需要每次都注意，但改代码时如果
新加了矩阵运算要记得避开）：
- 大矩阵-向量乘法 → 手写 `matvec()`：`(mat * vec).sum(axis=1)`，逐元素乘加代替 BLAS gemv。
- 3×3 对称矩阵最小特征值方向（原来的 SVD/eigh）→ 手写 Cardano 解析解 `smallest_eigvec_3x3()`。
- 3×3 行列式 → 手写余子式展开 `_det3()`，不用 `np.linalg.det`。
- 小的向量-向量运算（`np.dot`/`np.cross`/`np.linalg.norm` 用在3元素向量上）不受影响，可以正常用。

**建议**：找时间重装这个 conda 环境的 numpy（或换个 BLAS 后端，比如强制装 OpenBLAS 而不是当前
这个坏掉的库），彻底解决，而不是每处新代码都要手写绕开。`np.show_config()` 显示的是
"blas: name blas, openblas configuration: unknown"，装的具体是什么后端没能确认。
